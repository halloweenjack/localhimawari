# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクト概要

「ローカルひまわり」は、NICT（情報通信研究機構）が提供する気象・日射量データ可視化Webアプリケーション。ひまわり衛星画像、日射量(AMATERASS)、気温、湿度、風向風速、気象警報、AMeDASデータなどをMapbox GL JSベースの地図上にタイルオーバーレイ・マーカーとして表示する。

ビルドツール・バンドラーは使用しておらず、静的HTML + jQuery + vanilla JSで構成されている。

## アーキテクチャ

### 2層iframe構造

アプリケーションは親フレーム(`index.html`)と子フレーム(`map/index.html`)の2層構造。

- **親フレーム** (`index.html`): タイムライン制御UI（k2goTimeline）、再生コントロール、ズーム、ViewURL機能を担当
- **子フレーム** (`map/index.html`): Mapbox GL JSによる地図表示、タイルレイヤー管理、マーカー（気象警報・AMeDAS）表示を担当

### iframe間通信

親子間はwindow関数呼び出しで連携する（`js/STARScontroller.js` が橋渡し役）:
- 親→子: `$('#map')[0].contentWindow.updateDate()`, `.getLatestDate()`, `.getViewURL()`, `.setPosition()`, `.isReady()`
- 子→親: `window.parent.STARScontroller_setDate()`

### 主要JSファイル（親フレーム）

| ファイル | 役割 |
|---|---|
| `js/env.js` | グローバル設定 `$Env`（時間範囲、ズームテーブル、再生速度テーブル） |
| `js/main.js` | k2goTimelineの初期化、UI全イベントハンドラ（再生・ズーム・範囲バー・ViewURL） |
| `js/fnc.js` | ユーティリティ関数群（`adjustCurrentTime`, `adjustRangeBar`, `getZoomLevel`, `roundDate`） |
| `js/STARScontroller.js` | iframe間通信ブリッジ（`mapsync_updateDate`, `mapsync_getMapLatestDate`等） |
| `timeline/jquery-k2go-timeline.js` | k2goTimelineプラグイン（タイムラインUI） |

### 主要JSファイル（子フレーム: map/）

| ファイル | 役割 |
|---|---|
| `map/js/harps-env.js` | `$.harpsEnv` - 地図設定・タイルデータ定義（API URL、ズーム範囲、排他制御、凡例、気象警報コード体系） |
| `map/js/harps-model.js` | `$.harpsModel` - データモデル（タイル管理、時刻管理、LocalStorage） |
| `map/js/harps-model-localhimawari.js` | `$.harpsModelLocalHIMAWARI` - 気象警報・AMeDAS・AMATERASSポイント処理 |
| `map/js/harps-view.js` | `window.*` 関数群のエクスポート（親フレームからの呼び出しインターフェース） |
| `map/js/harps-view-localhimawari.js` | `$.harpsViewLocalHIMAWARI` - 警報リスト・AMeDASダイアログ等のView処理 |

### データソース

タイルデータはAPI経由で取得される（`/api/` prefix）:
- `amjp`: 日射量（amaterass_jp / amaterass_ao）
- `h8jp`: ひまわり衛星画像（himawari-8_ao）
- `wni`: 高解像度降水（weather_wni）
- `amjp_temp/humidity/wnd`: 気温・湿度・風（temperture/humidity/wind_jp/ao）
- `wwarn`: 気象警報（weather_warn）
- `amedas`: AMeDAS観測データ

## 開発

ビルドステップはない。静的ファイルをWebサーバーから配信するのみ。

ローカル開発時はAPIバックエンド（`/api/`）へのプロキシが必要。

## 注意点

- 地図表示にはMapbox GL JS v2.6.0を使用（アクセストークンの設定はコード内で行われている）
- jQueryのバージョンが親フレーム（3.6.0）と子フレーム（3.5.1）で異なる
- `$Env` は親フレームのグローバル設定オブジェクト、`$.harpsEnv` は子フレームの設定オブジェクト（別物）
- 時刻は分単位に丸められる（`roundDate`関数）
- タイルURLの生成関数内でズームレベル < 5 の場合に広域データ（`_ao` suffix）に切り替わる


<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

### Feb 9, 2026

| ID | Time | T | Title | Read |
|----|------|---|-------|------|
| #4581 | 10:55 AM | ✅ | Created CLAUDE.md documentation file | ~210 |
</claude-mem-context>